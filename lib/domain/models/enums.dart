/*
Enum that defines different categories of items in the shopping list.
Each enum value corresponds to a specific category, making it easier to
manage and reference these categories throughout the app
 */

enum CategoryEnums {
  vegetables,
  fruit,
  meat,
  dairy,
  sweets,
  convenience,
  other,
}
